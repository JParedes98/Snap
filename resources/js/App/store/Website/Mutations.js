export default {
    setWebsiteFilesList(state, list) {
        state.filesList = list;
    },

    setWebsiteSnippetsList(state, list) {
        state.snippetsList = list;
    },

    setWebsiteLinksList(state, list) {
        state.linksList = list;
    },
}
